apiVersion: v1
kind: Service
metadata:
  name: {{ include "m3.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "m3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
      appProtocol: mcp
  selector:
    {{- include "m3.selectorLabels" . | nindent 4 }}
