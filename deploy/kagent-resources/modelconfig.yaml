# ModelConfig for OpenAI
apiVersion: kagent.dev/v1alpha2
kind: ModelConfig
metadata:
  name: openai-model
  namespace: kagent
spec:
  provider: OpenAI
  model: gpt-4.1-mini
  openAI: {}
  apiKeySecret: kagent-openai
  apiKeySecretKey: OPENAI_API_KEY
