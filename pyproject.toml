[project]
name = "mcp-clinical-platform"
version = "0.1.0"
description = "MCP Clinical Platform — federated MCP + GitOps + AI agents"
requires-python = ">=3.10"
dependencies = [
    "ipykernel",
    "matplotlib",
    "pandas",
    "requests",
]
